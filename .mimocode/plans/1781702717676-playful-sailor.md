# Plan: Fix Pack Generator + Remove Compare

## Changes

### 1. Pack Generator (`src/pages/pack.astro`)
- Add dropdown suggestions (max 5) as user types in search input
- Remove "Flatpak (Universal)" from distro dropdown (Flatpak is used by default for all)
- Add info text explaining Flatpak is used for all installations
- Keep Ubuntu/Fedora/Arch options (they just change the Flatpak install command)

### 2. Navigation (`src/layouts/Layout.astro`)
- Remove Compare link from nav

### 3. Delete Compare page
- Remove `src/pages/compare.astro`
- Remove compare-related CSS from `src/styles/global.css` if any

## Files to modify
- `src/pages/pack.astro` - rewrite with suggestions dropdown
- `src/layouts/Layout.astro` - remove Compare nav link
- Delete `src/pages/compare.astro`

## Verification
- `npm run build` succeeds
- Pack page shows suggestion dropdown when typing
- No Compare link in navigation
